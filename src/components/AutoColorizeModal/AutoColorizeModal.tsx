// Stub component - PRO feature not available in open source version
interface AutoColorizeModalProps {
  isOpen: boolean;
  onClose: () => void;
}

export const AutoColorizeModal = (_props: AutoColorizeModalProps) => null;
